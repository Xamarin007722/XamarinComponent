using System;
using Xamarin.Forms;

namespace UtilityClasses.Behaviors
{
    public class GenericBehaviors:Behavior<Entry>
    {
        public GenericBehaviors()
        {
        }
        protected override void OnAttachedTo(Entry bindable)
        {
            base.OnAttachedTo(bindable);
        }
    }
}
